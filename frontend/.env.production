# Production deployment on Vercel (static frontend only)
# Backend API is not deployed - demo pages use seeded data via localhost
# For full deployment, set this to your backend host URL
VITE_API_URL=
